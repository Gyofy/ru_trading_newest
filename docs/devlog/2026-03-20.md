# 2026-03-20 Devlog: Feature Leakage Discovery + Strategy Pivot

## Summary

**가장 중요한 하루.** ML 파이프라인의 핵심 feature에서 데이터 누수를 발견하고, 전략을 근본적으로 재설계했다.

## 발견

### Feature Leakage (10:00)
- `_add_decomposition()`: STL의 centered MA가 미래 데이터 사용
- `ichi_above_cloud`: `.shift(26)` = 26바 미래 클라우드 참조
- SVD stride+interpolation: 보간이 미래 바 추가 시 값 변경

### Impact
- S2 방향 예측: **0.687 → 0.518** (누수 제거 후 거의 랜덤)
- v4.0~v4.3의 모든 백테스트 수익(+1.5%/trade)은 **누수의 산물**
- Mega Search v2/v3 결과 전부 무효

### Fix
```
crypto_ohlcv.py: STL → EMA(adjust=False) causal only
signal_features.py: ichi_above_cloud .shift(26) 제거
crypto_ohlcv.py: SVD stride/interpolation 제거
```

## 전략 탐색 (11:00~15:00)

### 전수 검색: 15,120 조합
- 9코인 × 12전략 × 7 TP/SL × 6 hold time
- **양수: BTC spike 관련만**, 나머지 전부 마이너스

### 검증
- "BTC 롱 편향" 기각 (하락장 -38.5%에서도 양수)
- Edge 원인: momentum continuation (lag 아님)
- 레버리지 3x 안전 (청산확률 ~0%)

### SOL/ADA 마이크로스트럭처
- 캔들/볼륨/바크기 패턴 300+ 조합: 전부 단독 마이너스
- BTC spike + 알트 확인만 양수 (SOL WR 56.7%)

## 결과물

### 코드 변경
- `crypto_ohlcv.py`: 누수 수정
- `signal_features.py`: 누수 수정
- `live_predictor.py`: `predict_hybrid()` 추가
- `run_live_bot_v2.py`: deadzone, kill switch 조정
- `frozen_params_v4_3.yaml`: rf=2%, k_lower=1.0, deadzone=0.10

### 새 봇
- `run_btc_spike_paper.py`: BTC spike paper trading bot
- 확인 필터 (볼륨/바크기/방향/바디) + score 사이징

### 분석 스크립트
- 13개 분석 스크립트 (`scripts/`, `rjh_20260320/`)
